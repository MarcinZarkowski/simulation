# Not the dependency source of truth. pyproject.toml is.
#
# This file previously pinned 62 packages -- the full transitive closure of a
# Streamlit app -- while the maintained package imports two. Keeping both in sync
# by hand does not work, so this now just points at the real declaration.
#
#   uv sync                        runtime + dev
#   uv sync --extra legacy-dashboard   adds the predecessor dashboard's deps
-e .
